#!/bin/sh

# This script gets copied in the release bin/jvm directory. We can find
# francis.jar relative to that directory.
script_dir="$(realpath "$(dirname "$(readlink -f "$0")")")"
java -jar "$script_dir/../../jar/francis.jar" "$@"
